<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GPlanner.Maui.Pages.TasksPage"
             Title="Tasks Page"
             xmlns:viewModel="clr-namespace:GPlanner.Maui.ViewModels"
             x:DataType="viewModel:TasksViewModel"
             xmlns:dto="clr-namespace:GPlanner.Maui.Services.Dtos"
             x:Name="TasksPageRoot">
  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Add item"
                 IconImageSource="add.png"
                 Command="{Binding OpenTaskModalCommand}"/>
  </ContentPage.ToolbarItems>

  <Grid RowDefinitions="Auto,*"
        Padding="10"
        RowSpacing="10">

    <SearchBar Grid.Row="0"
               Placeholder="Search tasks..."
               Text="{Binding SearchText, Mode=TwoWay}"
               BackgroundColor="#F0F0F0"
               FontSize="16"
               TextColor="#202124"/>

    <CollectionView Grid.Row="1"
                    ItemsSource="{Binding FilteredTasks}">
      <CollectionView.ItemTemplate>
        <DataTemplate x:DataType="dto:UserTaskReadDto">
          <SwipeView>
            <SwipeView.RightItems>
              <SwipeItems Mode="Reveal">
                <SwipeItem Text="Delete"
                           IconImageSource="delete.png"
                           Command="{Binding Source={x:Reference TasksPageRoot}, Path=BindingContext.DeleteCommand}"
                           CommandParameter="{Binding .}"/>
              </SwipeItems>
            </SwipeView.RightItems>

            <Frame CornerRadius="16"
                   HasShadow="True"
                   BorderColor="#E0E0E0"
                   BackgroundColor="White"
                   Padding="16,12"
                   Margin="0,4">

              <Grid RowDefinitions="Auto,Auto"
                    ColumnDefinitions="*,Auto">
                <Label Grid.Row="0"
                       Grid.Column="0"
                       Text="{Binding Title}"
                       FontSize="18"
                       FontAttributes="Bold"
                       TextColor="#202124"
                       VerticalOptions="Center"/>

                <ImageButton Grid.Row="0"
                             Grid.Column="1"
                             Source="edit.png"
                             BackgroundColor="Transparent"
                             WidthRequest="18"
                             HeightRequest="18"
                             Command="{Binding Source={x:Reference TasksPageRoot}, Path=BindingContext.OpenTaskModalCommand}"
                             CommandParameter="{Binding .}"
                             HorizontalOptions="End"
                             VerticalOptions="Center"
                             Padding="4"/>

                <StackLayout Grid.Row="1"
                             Grid.Column="0"
                             Grid.ColumnSpan="2"
                             Orientation="Horizontal"
                             Spacing="8"
                             Margin="0,6,0,0">
                  <Label Text="{Binding Date, StringFormat='{0:dd MMM yyyy HH:mm}'}"
                         FontSize="13"
                         TextColor="#777"/>
                  <Label Text="•"
                         FontSize="13"
                         TextColor="#CCC"/>
                  <Label Text="{Binding Type}"
                         FontSize="13"
                         TextColor="#777"/>
                </StackLayout>
              </Grid>
            </Frame>
          </SwipeView>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>

  </Grid>
</ContentPage>